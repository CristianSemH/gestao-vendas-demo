import React from 'react';
import FormLogin from '../../components/Login/Form';

const LoginIndex = ({ setIsLogin }) => {
    return (
        <FormLogin setIsLogin={setIsLogin}></FormLogin>
    )
};

export default LoginIndex;